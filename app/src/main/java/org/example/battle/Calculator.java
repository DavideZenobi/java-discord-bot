package org.example.battle;

import org.example.pokemon.Move;
import org.example.pokemon.Pokemon;

public class Calculator {
    

    public void calculateDamage(Pokemon attackingPokemon, Pokemon defendingPokemon, Move move) {

        // (((2 * level / 5 + 2) * power * attackStat / defenseStat) / 50 + 2) * targets = 1 * PB = 1 * weather = 1 * glaiveRush = 1 * critical = 1 * random(85...100 / 100) *
        // STAB = 1 * type = 1 * burn = 1 * other = 1
    }
}


/*
 * 
 */